interface:
  display_name: "Design Buff"
  short_description: "用业务与体验视角做完整设计审核"
  default_prompt: "使用 $design-buff 审核这份设计，从产品和体验角度还原场景与目标；有 Figma 证据时优先走高精度读取；遵循内置的 intake、report 与 review-state 合同，并在对话里先给出精炼启动总结。"

dependencies:
  tools:
    - type: "mcp"
      value: "figma"
      description: "Figma MCP 服务"
      transport: "streamable_http"
      url: "https://mcp.figma.com/mcp"
